#include <iostream>
using namespace std;
int main() {
    
    int Num[10], i;
    int soma = 0;
    int maior, menor;

    for(i=0; i<10; i++) {
        cout << " posiçao " <<i<< " do vetor ";
        cin >> Num [i];     
    }   
    maior = Num[0];
    menor = Num[0];
    
    for (i = 1; i<10; i++){
        
    if (i==0){
        maior = menor = Num [i];}
        else {
            if ( Num[i] > maior) maior = Num[i];
            if ( Num[i] < menor) menor = Num[i];
        
    }
    }
    cout << "numero maior eh " << maior << " E o Numero menor eh " << menor;
    cout <<"\n\n";
    for (i = 0; i < 10; i++) {
        cout << Num [i]<< "\t";
    
    }
}
